on: [push]  # => Quand C'est push.

jobs:
  deploy_job:
    runs-on: ubuntu-latest
    name: deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Deploy file
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: u34798713 # => mot de passe du server.
          server: access914943276.webspace-data.io 
          port: 22 # => Port du server.
          local_path: './*' # => le fichier à modifier.
          remote_path: '/portfolio/' # => le dossier/chemin d'accès.
          sftp_only: true # => sftp obligatoire
          password: GARCIAort2023 # => mot de passe du server.
